<h1>Lost Items</h1>

<ul>
 <% @items.each do |item| %>
 <li>
   Name:<%= link_to item.name, item_path(item) %>
   Category: <%= item.category %>
 </li>
 <% end %>
</ul>

<%= link_to "New Item", new_item_path %>
